# Contracts: サイドバーとビューアのスクロール動作修正

本機能はUIレイアウトとスクロール制御のみを扱い、新規の外部APIやバックエンドエンドポイントの追加はありません。既存のTauriコマンドやファイルシステムAPIに変更は不要です。

- 新規エンドポイント: なし
- 既存エンドポイントの変更: なし
- クライアント間契約: レイアウト挙動（サイドバーは独立スクロール、ビューアはズーム時のみスクロール）を満たすこと

必要に応じてUI契約テスト（jsdomによるDOM属性・クラス検証）で担保します。
