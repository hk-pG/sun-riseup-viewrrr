[package]
name = "core_logic"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
zip = "8.4.0"

[dev-dependencies]
uuid = { version = "1.22.0", features = ["v4"] }
