# Dev Todo

todo for minimal iterate.

## Grand Goal

**圧縮ファイルをコンテナとして扱えるようにする**

## what is `container`

ビューアで選択可能な最小単位。

中に複数の画像ファイルを内包できる。

### 圧縮ファイルを`コンテナとして扱う`とは？

#### フォルダと同様に扱えるように、圧縮ファイルのパスに対して解凍結果を紐づけること。

**TODO**

- [x] 外部から解凍する一時ディレクトリを受け取る
- [x] 元の圧縮ファイルパスからハッシュ値を作成する
- [x] ハッシュ値を解凍ディレクトリ名として解凍する
- [x] 圧縮ファイルパスへのアクセスを、解凍した一時ディレクトリへバイパスする

#### フォルダと同様に、内包している画像からサムネイルを取得できること。

**TODO**

- [ ] STEP1同様に、圧縮ファイルのパスと解凍結果を紐づける
- [ ] 元の圧縮ファイルパスのサムネイルを要求された時、解凍結果フォルダにバイパスする
